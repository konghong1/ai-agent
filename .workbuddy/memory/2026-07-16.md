# 2026-07-16 工作日志

## 出图规划列表标签修复（极速出图 / 自定义）
- 现象：除「活动海报」外，推荐类型全被错误显示成「自定义」标签。
- 根因：`index.tsx` 用 `isFast = !!t?.fast` 判标签，但后端 `PLAN_TYPES` 仅 `promo` 标 `fast:True`，其余推荐类型默认 False。
- 修复：`web/src/pages/EcommerceGallery/index.tsx` 第 ~1182 行改为 `isFast = !isCustom`（推荐类型=极速出图，自定义子任务=自定义）；比例默认回退保留 `typeFast = !!t?.fast`，避免误改未提及展示。
- 验证：`npm run build` 通过；`docker restart ai-agent-web` 后 localhost:80 返回新 bundle（index-BUv27qke.js）。沙箱无无头浏览器，未能截图，需用户 Ctrl+F5 目视确认。
- 提示：PlanRow 的 `fast` prop 同时控标签文字/左侧边框色/比例默认回退；标签语义现在=推荐vs自定义，与后端 `t.fast` 已解耦。

## 生成图片统一上传 MinIO + 新增「删除任务」功能
- 核查结论：原「立即生成」的图片**全部落本地盘** `uploads/gallery/`（经 `/api/gallery/files/` 回显），**未进 MinIO**。
- 改造（统一 MinIO）：
  - `app/gallery_service.py` 新增 `_save_to_storage(data, ext)`：优先 `get_storage_backend().put` 上传 `ai-agent-minio`（key=`gallery/results/{uuid}{ext}`），失败回退本地；`_save_generated_image`/`_persist_overlay_result`(规格叠加) 改用它。
  - `_real_generate` 与 `run_gallery_task._generate_one` 的 spec 叠加结果都经 `_persist_overlay_result` 落 MinIO。
  - `gallery_routes.py` 的 `serve_file` 改「先 MinIO.get，再本地 resolve_file 回退」，页面仍走同源 `/api/gallery/files/{key}` 取图（含历史本地文件兼容）。
- 新增删除任务：`gallery_service.delete_task`（删 MinIO 对象 + 本地回退 + 级联删 records/配置快照，校验 user_id 数据隔离）；`gallery_routes.py` 新增 `DELETE /api/gallery/tasks/{task_id}`(204/404)；前端 `gallery.ts.deleteTask` + `index.tsx` 任务卡片头部新增删除按钮(`handleDeleteTask`，window.confirm 确认，本地列表过滤释放内存)。
- 验证：后端 py_compile 通过；前端 `npm run build` 通过；`docker restart api/worker/web` 启动无错；`DELETE` 路由注册(无 token 返回401)；在 api 容器内 `get_storage_backend()` 跑通 MinIO put/get/delete 闭环（key=`gallery/results/_probe.png`）。注意：api 容器内 `MINIO_ENDPOINT=minio:9000`（compose 已配），宿主机测试用 localhost:9000。
- 风险点：删除“进行中”任务存在极小竞态（worker 在 delete 后刚上传的 MinIO 对象可能成孤儿），属可接受；如需彻底可加 worker 取消信号。

## 删除任务交互约束收紧（用户明确要求）
- 需求：①**进行中（pending/running）的任务禁止删除**；②**删除必须弹框二次确认**。
- 后端：`gallery_service.delete_task` 在 ownership 校验后新增 `if task.status in ("pending","running"): raise ValueError("任务正在进行中…暂不可删除")`；`gallery_routes.remove_task` 用 `try/except ValueError → HTTPException(400)` 包裹（404=不存在，400=禁止操作，前端能区分）。
- 前端：`index.tsx` 引入 `@ant-design/icons` 的 `ExclamationCircleFilled`；`handleDeleteTask` 由 `window.confirm` 改为 antd `Modal.confirm`（danger 按钮、展示任务名+影响范围、cancelText=取消，onOk 内 try/catch 把 `e.response.data.detail` 透出给用户）；并加 `if (pending||running) message.warning + return` 防御。
- 按钮：任务卡片头部 `task-del-btn` 增加 `disabled={running}`（running=pending||running），`gallery.css` 加 `.task-del-btn:disabled` 置灰样式；hover title 区分“进行中不可删 / 可删”。
- **附带收益**：禁止删进行中任务，从源头消掉了上次提到的“删除与上传竞态”孤儿对象问题，无需给 worker 加取消信号。
- 验证：后端 py_compile 通过；前端 `npm run build` 通过；`docker restart ai-agent-api ai-agent-web` 启动无错；容器内直接跑 `delete_task` 三场景——pending 拦截✓、running 拦截✓、completed 删除成功(True, 记录消失)✓；测试任务已清理。

## 任务 31 排查：卡在「创作中」的僵尸 running
- 现象：用户反馈任务 31 一直「创作中」。核查：task 31 `status=running, done=0/18, failed=0, records=0`，`created_at==updated_at==2026-07-15 10:01:20`（进度字段自创建从未变）；同用户任务 30(18/18)、29~26 均 completed，**唯独 31 卡死 24h+**。
- 根因：`gallery_worker` 用 api 进程内**内存队列**（`queue.Queue()`）。`docker restart ai-agent-api`（历史迁移 restart loop + 今日多次重启）清空队列+worker 线程死亡 → 原 running 任务**永久僵尸**：没人接管、进度永远 0、也不标 failed（hang 不抛异常/新进程不知有此任务）。属「假 running」，非真失败。
- **如何判断失败（回答用户）**：① `status='failed'`+有 `error`=真失败（worker 跑完抛异常/预检查拦截）；② `status='running'` 但 `done/failed/records` 长时间不变+`created_at` 陈旧=**卡死/僵尸**（任务 31 即此）；③ `status='completed'/'partial'`=成功。
- 修复：① `gallery_worker._recover_orphans` 启动恢复孤儿 running（带 PROCESS_START-120s 阈值防活跃任务重复生成）；② `delete_task` 卡死 running（created>30min）放行删除；③ 前端 `isStuck` 提示+卡死可删。
- 验证：后端 py_compile 通过；前端 `npm run build` 通过；`docker restart api/web`；容器内 `delete_task` 三场景——卡死 running 放行(True)✓、活跃 running 拒绝✓、pending 拒绝✓；任务 31 仍在(running done=0)未被误删，用户 Ctrl+F5 后前端删除按钮可用，点确认即清理。
- 注意：任务 36（running, created 03:06）暴露「无阈值 recovery 会重入队活跃任务→重复生成」隐患，已用 PROCESS_START-120s 阈值修复（36 当时已消失，未造成重复数据）。

## 修复：删除按钮「点击没反应」（UX 陷阱）
- 现象：用户点任务删除按钮无反应。
- 诊断（纠偏）：最初怀疑 `created_at` 解析失败致 `isStuck` 误判 disabled，但核实后端真实 JSON `created_at` 是标准 ISO `2026-07-15T10:01:20`（带 T），浏览器 `new Date()` 可解析；无全局 json_encoders。故**解析不是根因**。真正根因是「进行中（running/pending）任务按钮硬 `disabled` 但 HTML 静默无反馈」——要么用户浏览器在跑旧 JS（旧代码 `disabled={running}` 对所有 running 恒禁用、含卡死任务31），要么点的是活跃进行中任务被静默禁用，用户以为坏了。
- 修复（`index.tsx` + `gallery.css`）：移除按钮 `disabled` 属性，改为**可点击**。活跃进行中（running 非卡死 / pending）按钮加 `.is-blocked` 灰态视觉，点击时 `handleDeleteTask` 内 `message.warning('任务进行中，暂不可删除')` 给明确反馈；卡死 running（>30min）与 已完成/失败/partial 点击弹 `Modal.confirm` 确认框。保留后端 400 硬拦截保证「进行中不让删」。
- 验证：前端 `npm run build` 通过（新 bundle `index-BOHTLjwM.js`，4204 modules）；`docker restart ai-agent-web` 后 web=200、index.html 引用新 bundle、dist JS 含 `is-blocked` 逻辑(grep=1)。
- **关键交付点**：此修复必须配合用户 **Ctrl+F5 强刷**（SPA 内存里旧 JS 才更新），否则仍跑旧代码。

## 🔥 实锤：删除按钮"没反应"的真根因 = React19 + antd 缺兼容补丁（覆盖上一条诊断）
- **上一条诊断（"UX 陷阱/硬 disabled"）是错的**：用户强刷后仍没反应，证明不是禁用或旧 JS。我改用 Playwright + chromium 真机实测（详见下"测试工作流"），抓到铁证：
  - 点击**成功触发** `handleDeleteTask`（CLICKED，无报错）；
  - 但 **`Modal.confirm` 弹框没出现**（MODAL_APPEARED=false）、**`message.warning` 提示也没出现**（MESSAGE_TEXT 空）；
  - 控制台/页面**零错误**。
  → 即 antd 静态 `message`/`Modal` 方法被调用但**静默不渲染、也不报错**。
- **真根因**：Web 用 `react@19.2.7` + `antd@5.15.4`，antd v5 为 React 18 构建；缺 `@ant-design/v5-patch-for-react-19` 时，antd 静态 `message.xxx`/`Modal.confirm` 在 React 19 下静默失效。**全站所有用静态 message/Modal 的地方此前大概率都坏了**（不止删除按钮）。
- **修复**：`npm i @ant-design/v5-patch-for-react-19`；`web/src/main.tsx` **第一行** `import '@ant-design/v5-patch-for-react-19'`（须在 import antd 的 App 之前）。已写入 `web/package.json` 依赖。
- **真机回归（Playwright 端到端，已通过）**：注入 konghong token（admin 重置其密码为 Test123456 后登录）→ 进 `/ecommerce-gallery` → 点任务 31 删除按钮 → **弹框出现(MODAL_APPEARED=true)** → 点「删除」确认 → **任务 31 从列表消失(DELETE_SUCCESS=true)**，零 console/page 错误。截图 `verify-shots/modal_shown.png`、`after_delete.png`。
- **结论**：之前两次"修好了"都未做真机验证，靠猜 → 这次用浏览器实测才抓到真因。已把"改动必须测试/回归通过"写入项目永久记忆；并沉淀浏览器 E2E 工作流（Playwright 注入 localStorage `agent-auth`）。
- 注意：为测试我把用户 konghong(kh1763751448@gmail.com) 密码经 admin 重置为 Test123456，已告知用户可自行再重置；测试 token 文件已删除。

## 修复：聊天会话列表排序 —— 新建会话应排在最下（创建时间正序）
- 现象：用户反馈聊天时新建会话的排序不对，要求**新建的会话排在最下面**（历史会话在上、新的在下）。
- 根因：最初我把后端 `list_threads` 改成了 `order_by(Thread.created_at.desc())`（新在上），而用户实际想要的是**正序**（新在下）。另外，前端通过 `setThreads(prev => [data, ...prev])` 乐观插入时也会把新会话插到最前，和后端正序口径不一致。
- 修复：① 后端 `app/api.py` 两处 `order_by(Thread.created_at.desc())` → `order_by(Thread.created_at.asc())`（最新创建的会话在最下，按创建时间正序）；② 前端 `ChatInterface/index.tsx` `fetchThreads` 拿到数据后按 `created_at` 兜底正序 `setThreads([...data].sort((a,b) => a.created_at - b.created_at))`；③ 新建会话（`handleNewThread` 与发送消息时自动创建）的乐观插入改为 `setThreads(prev => [...prev, data])`（追加到末尾）。`data-thread-id` 选择器保留，方便回归定位。
- 验证（两层，均通过）：
  - 后端接口验证：真实 `/api/threads` 返回 `created_at` 严格非递减（旧->新），老会话 `根据图片` 在最上，新会话 `E2E_B` / `新会话` 依次在下 ✅。
  - 真机浏览器 E2E（`playwright_chat_ascending_e2e.cjs`）：进 `/agents/chat` → 点「新建会话」→ 新会话 `thread-0669cb9ce24a` 出现在列表**最底部**，零 console/page 错误 ✅。截图 `verify-shots/chat_ascending_e2e.png`。
  - 构建：`npm run build` 通过；`docker restart ai-agent-api ai-agent-web` 后 web=200，api 无导入错误。
- 教训：列表排序类需求必须确认用户想要的「方向」——正序 vs 倒序、新在上 vs 新在下，不能靠猜；我前一次理解反了，已纠正。

## 修复：电商套图「重做」点完直接显示失败、无进度（🔥ORM 对象误传后台线程）
- 现象：用户点单图「重做」→ 图片不显示「生成中」进度，直接显示「生成失败」。
- 根因（两层，均在 `app/gallery_service.py` 的 `regenerate_record`）：该函数把请求线程里的 ORM 对象（用户传 `user`、记录 `rec`）直接交给了后台线程 `_do` 去用。请求一返回，`get_db` 会话关闭 → `user`/`rec` 变成 **detached/expired**：
  1. 第一层：`_do` 里 `user.id` 触发懒加载 → `DetachedInstanceError: Instance <User> is not bound to a Session`；
  2. 第二层（修掉第一层后暴露）：`_do` 里 `rec.title` 触发懒加载 → `InvalidRequestError: This session is provisioning a new connection; concurrent operations are not permitted`。
  两处异常都被 `regenerate_record._do` 的 `except` 捕获 → 记录被误置 `status='failed'`、`result_url=None`。毫秒级崩，前端轮询 2.5s 后只看到 `failed`，故“没进度、直接失败”。
- 修复（与代码既有模式 line 1606 `user_id = user.id # 关闭会话前提取` 一致）：在**主线程、会话还活着时**把后台线程要用的所有 ORM 字段取成普通变量——`uid = user.id`、`rec_title = rec.title`、`rec_type_id = rec.type_id`，`_do` 内一律用这些本地变量，绝不再碰 `user`/`rec`。前端 `doRedo` 提交成功后立即 `setTasks` 把该记录乐观标为 `processing`（带 `data-record-id` 的单元格属性，便于 E2E 锁定）。
- 验证（两层均通过）：
  - 后端复现+修复：对 rec 93 触发 regenerate，修复前日志报 `DetachedInstanceError` → `provisioning` 错误且记录瞬变 `failed`；修复后记录稳定 `processing`(约15s)→ `completed`(带真实图片 URL)，日志零相关错误 ✅。
  - 真机浏览器 E2E（`playwright_redo_e2e.cjs`）：进 `/ecommerce-gallery` → 点已完成图 rec#209 的「重作」→ 输提示词→「重新生成」→ 同一张单元格立刻显示 `生成中 · 第 10 张`(进度)，`failed=false`，零 console/page 错误 ✅。截图 `verify-shots/redo_e2e.png`。
- 教训：**绝不要把请求作用域的 ORM 实例（User/Record 等）传入后台线程/异步任务**，请求返回后其会话即关闭，再读属性会 lazy-load 并抛 detached/并发错误。正确做法：在主线程把需要的标量字段（id/title/type_id…）先取成普通变量再闭包传入。此坑与「FastAPI startup 不做阻塞网络调用」同属会话生命周期类，已并入 MEMORY.md 硬约束。

## 修复：电商套图「很多图片一直生成中」永久卡死（🔥双层根因）
- 现象：用户反映大量图片（任务 38/39 等）一直「生成中」很久不动，刷新无效。
- 根因（两层叠加，缺一不可）：
  1. **worker 不在进程启动时启动**：`gallery_worker.start_worker()` 原先只写在 `enqueue_task()`（懒启动，靠「用户创建新任务」触发）。api 一重启，内存队列清空；若重启后短期内无人创建新任务，worker 线程与孤儿恢复永不运行 → 历史 running 任务（38/39）永久卡死。api 日志无任何 `run_gallery_task` 调用印证。
  2. **resume 仍走 chat 提示词生成**：`run_gallery_task` 阶段1 无条件先调 `_build_prompts_for_plan`（agnes chat 接口，`AI_PROMPT_TIMEOUT=120s`）。即使孤儿恢复接管了历史任务（已有 `prompt_en`），resume 也会卡死在 chat 120s 超时 → 单线程 worker 一旦卡住，所有任务冻死。上游 agnes chat 接口当时不可达/饱和（诊断进程卡 95s+ 超时杀）。
- 修复（四处，均 bind mount，重启 api 生效）：
  1. `server.py` `startup()` 主动调 `gallery_worker.start_worker()`（只启线程+查本地DB，无网络，符合约束）。
  2. `gallery_worker._recover_orphans` 判定改 `running AND updated_at < PROCESS_START-120s`（按最后进度，活跃任务不被误抢）。
  3. `gallery_service.run_gallery_task`：阶段1 仅在「全新任务（无 existing record）」时调 chat；resume/孤儿场景复用 `rec.prompt_en` 直接出图，彻底绕过当前不可用的 chat 接口。并新增「resume 复用已有 record、跳过已完成/失败、不重复预建」逻辑（`run_gallery_task` 末尾按 record 实际状态重新统计 done/failed/status，resume 不清零已完成数）。
  4. `gallery_worker` 改 `ThreadPoolExecutor(max_workers=2)` 并发消费队列 + `gallery_prompt_ai` chat client `max_retries=1`（减少重试累积超时）。
- 验证（两层均通过）：
  - 后端：重启后日志出现 `gallery background worker started` + `恢复孤儿生成任务 task=38/39`；轮询 task 38 由 `completed=9 卡死` → `completed 18/18`（彻底跑完），task 39 由 `completed=8` → `partial 17/18`（17 完成，1 张因上游 agnes 间歇 `SSLEOFError`/`queue-full` 失败，可重做）。
  - 真机浏览器 E2E（`playwright_gallery_fix_e2e.cjs`）：进 `/ecommerce-gallery`，DOM 中 task 38 的 18 个 record cell 全部 `domCompleted=18, domBusy=0`，task 39 的 17 completed+1 failed，`domBusy=0`，**PAGE_ERRORS=0** → 前端真实渲染完成图、不再「生成中」 ✅。截图 `verify-shots/gallery_fix_e2e.png`。
  - `py_compile` 四文件均 OK；`docker restart ai-agent-api` 后无导入错误。
- 注意（外部因素）：agnes chat/image 接口当前自身不稳定（chat 卡 120s、image 间歇 SSL EOF/队列满），属上游问题；代码侧已用 resume 绕过 chat + 并发 worker + media_retry 兜住，个别图失败属正常，用户点「重做」用原提示词可绕过 chat 重出。
