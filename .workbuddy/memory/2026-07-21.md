# 2026-07-21

## 聊天每轮性能优化（plan-chat-perf-v2）实施 + 验证
- 按 `designs/plan-chat-perf-v2.md` 落地：§1.1 工具池缓存+事件失效、§1.2 Catalog 瘦身、§1.3 KB 前置门控、§2.1 Intent Router 三档、§2.2 按需 KB 工具、§2.3 top-k 剪枝。
- 新增 6 个开关到 `app/settings.py` + `docker-compose.yml`（均带独立回退）。
- `app/mcp_tools.py`：工具闭包改自开 SessionLocal（`_call_mcp_tool`），解决缓存跨请求复用时的会话关闭问题；`invalidate_tool_pool` 接入 api.py MCP CRUD。
- `app/agent.py`：T0 用「整句仅问候」正则 + 排除实时意图，避免「你好，帮我查天气」被误判 T0 漏工具；`_prune_tools` 中文按字级重叠（原设计按词切在中文塌成一个 token 导致剪枝失效）。
- 测试：`tests/test_chat_perf_v2.py` 10/10 通过；相关回归 15/16（`test_agent_extensions` 那 1 个为预存失败，假 LLM 无 `.stream`，与本次无关）。
- 容器：`docker restart ai-agent-api` 干净启动（MySQL OK）；`/chat` 200、`/chat-stream` 200，日志确认 `intent router: tier=tools` 与 `tool pool HIT`。

## 聊天并发能力诊断（2026-07-21 上午）
- 用户问：「我这个 app 支持多少人同时在线聊天？？」
- **当前架构**：单进程 uvicorn（无 `--workers`） + LLM 同步阻塞；
- **同时在线（不发消息）**：可支持数百人（空闲连接不占 CPU）；
- **同时发消息**：**只能 1 个人**（LLM 调用阻塞整个进程）；
- **问题**：一个复杂请求卡住 → 其他请求全部排队等待。
- **三个设计缺陷**：①单进程 uvicorn；②没有超时隔离（LLM 不响应等满 120s）；③没有熔断/降级。
- **建议立刻改**：①分层超时（connect 10s, read 60s）；②多进程 uvicorn（`--workers 4`）。

## 并发能力修复（2026-07-21 中午）
- 用户质疑：「用自己的 API key 为什么还会互相影响？」「只支持一个人同时在聊天是怎么想的？」
- **根因**：单进程 uvicorn + 同步阻塞调用 → 一个请求占用整个进程 → 其他请求排队。
- **修复**：
  1. `docker/docker-compose.yml`：`--workers 4` → 可同时处理 4 个并发请求；
  2. `app/agent.py`：分层超时 `httpx.Timeout(connect=10.0, read=60.0, write=30.0, pool=10.0)` → 请求不会无限卡住。
- **验证**：并发测试通过（两个请求并行处理，总耗时 ≈ max(请求1, 请求2)）。
- **效果**：用户 A 的请求卡住，不会影响用户 B/C/D。

## 聊天超时失败提示优化（2026-07-21 下午）
- 用户需求：「聊天都卡死了，等待回复的动画孩子，要告诉用户是否失败」「回复可能需要执行多步，每步超时时间短些，但每次收到回复可重置」
- **实现**（双保险超时 + 活动重置）：
  1. `app/api.py` `/chat-stream`：
     - **chunk_timeout**: 60秒无新chunk视为卡死（精准判定）
     - **total_timeout**: 180秒总时间兜底（防无限跑）
     - **重置机制**：收到delta时重置`_last_chunk_time`，容忍"慢但有响应"的多步任务
     - **心跳提示**：每10秒返回`{"status": "正在处理中...(X秒)"}`让用户知道还在跑
  2. `app/agent.py` `_stream_once`：chunk级超时监控（60s无新chunk抛TimeoutError）
  3. 错误提示友好化：超时→「网络请求超时」；连接失败→「无法连接AI服务」；认证失败→「API认证失败」
- **设计优势**（对比之前的90秒绝对超时）：
  - ✅ 容忍"慢但有响应"：多步任务每步60秒，总时长可达3分钟
  - ✅ 精准判断卡死：60秒无chunk才判定失败，而非90秒绝对时间
  - ✅ 防恶意占用：180秒总超时兜底，无法无限跑
- **验证**：
  - 功能完整性：简单聊天HTTP 200正常返回
  - 超时提示：60秒无chunk返回`{"error": "LLM响应超时，请稍后重试。"}`
  - 心跳提示：等待期间每10秒返回`{"status": "正在处理中...(X秒)"}`
  - 并发能力：3个并发请求并行处理（多进程生效）
- **用户可见变化**：
  - 聊天不再无限卡死：60秒无响应→明确失败提示
  - 等待期间有进度反馈：用户知道"还在处理"而非"卡死了"
  - 多步任务更友好：每步有60秒缓冲，总时长可达3分钟

## Token 统计与上下文压缩（2026-07-21 晚）
- 用户需求：「max_tokens 截断需要检测」「上下文用量可视化（圆环）」「超限自动压缩」
- **实施**（全栈实现）：
  1. **后端**：
     - `requirements.txt`：添加 `tiktoken>=0.7.0`（OpenAI官方token计数库）
     - `app/token_counter.py`：TokenCounter类（按类别统计：system_prompt/tools/messages/mcp/skills）
     - `app/context_compressor.py`：ContextCompressor类（80%阈值触发，保留最近4轮，LLM摘要压缩）
     - `app/agent.py`：集成token统计 + 压缩 + `max_tokens`截断检测（`finish_reason == 'length'`）
  2. **前端**：
     - `web/src/components/TokenRing.tsx`：圆环组件（颜色随用量变深：绿→黄→橙→红）
     - `web/src/pages/ChatInterface/index.tsx`：处理 `token_usage` 事件，在发送按钮旁显示圆环
- **验证**：
  - Token统计：测试通过（"Hello, world!" = 4 tokens，消息列表正确计数）
  - 前端构建：成功（19.76s，无错误）
  - 功能完整性：圆环显示、点击弹窗、分项明细、超限提示
- **数据流**：
  ```
  用户发送消息 → ask_agent_stream_gen
    ↓ 计算 token usage → yield ("token_usage", {...})  ← 前端更新圆环
    ↓ 判断是否超限 → 超限则压缩 → yield ("status", "压缩中...") → 重新计算
    ↓ 调用 LLM.stream()
    ↓ 每个 chunk: yield ("delta", text)  ← 前端逐字显示
    ↓ 检测 finish_reason == 'length' → yield ("warning", "回答被截断")
    ↓ 结束 → yield ("done", {...})
  ```
- **用户可见变化**：
  - 每次聊天后显示上下文用量（圆环）
  - 点击圆环查看分项明细（系统提示词、工具、消息、MCP、技能）
  - 使用率≥80%时自动压缩历史对话
  - max_tokens截断时明确提示"回答被截断"

## 聊天架构重构设计（2026-07-21 下午）
- 用户需求：「基于 `learn-claude-code` 重构聊天架构，去掉无条件 RAG、删除硬编码 Intent Router、工具池首次加载后缓存、Hook 系统管理扩展点」
- **设计理念**（Claude Code）：
  - Agency comes from model, not from code orchestration
  - 模型决定何时调用工具，代码只执行模型请求
  - Hook 围绕循环，不改循环
- **核心变化**：
  1. **删除无条件 RAG**：知识库检索改为 `retrieve_knowledge` 工具，模型按需调用
  2. **删除 Intent Router**：T0/T1/T2 分流逻辑删除，统一为单一 agent_loop
  3. **Tool Pool 缓存**：MCP 连接 + Skills manifest 首次加载后缓存，配置变更时失效
  4. **Hook 系统**（基于 s04_hooks）：
     - UserPromptSubmit：用户提交消息时（注入上下文）
     - PreToolUse：工具执行前（权限检查、日志）
     - PostToolUse：工具执行后（输出处理）
     - Stop：响应生成后（会话统计）
- **架构图**：见 `designs/plan-chat-architecture-refactor.md`
- **预期收益**：
  - 简单聊天延迟：3-5s → 1-2s（-50%）
  - 知识库聊天延迟：8-15s → 5-8s（-40%）
  - 工具调用延迟：每次 500ms → 首次 500ms，后续 0ms（-90%）
  - 代码复杂度：1200+ 行 → 300 行核心循环（-75%）
- **下一步**：实施 Phase 1（Hook 系统）→ Phase 2（agent_loop 重构）→ Phase 3（Tool Pool）→ Phase 4（API 集成）→ Phase 5（测试验证）

## 聊天架构重构实施（2026-07-21 晚上）
- **完成情况**：5 个 Phase 全部完成 ✅
- **Phase 1: Hook 系统**
  - 创建 `app/hooks.py`：HOOKS 注册表、register_hook/trigger_hooks、内置 Hooks（permission_hook, log_hook, large_output_hook, context_inject_hook, summary_hook）
  - Hook 模型已存在于 DB（`models.py` 第 366-382 行）
- **Phase 2: agent_loop 重构**
  - 创建 `app/agent_loop.py`：统一的 agent_loop 函数，融入 Hook 调用点
  - TOOL_HANDLERS 注册表（bash, read_file, write_file）
  - build_messages_from_history 工具函数
- **Phase 3: Tool Pool 缓存**
  - 创建 `app/tool_pool.py`：ToolPool 单例类
  - 缓存 MCP 工具 + Skills + retrieve_knowledge
  - invalidate() 失效机制（配置变更时触发）
  - get_stats() 监控接口
- **Phase 4: API 集成**
  - 修改 `app/api.py` `/chat-stream` 端点：架构选择开关（V2/V1）
  - 创建 `app/agent_v2.py`：ask_agent_v2_stream_gen、should_use_v2_architecture
  - 添加 settings.py 字段：use_agent_v2、enable_knowledge_tool
  - MCP CRUD 触发 ToolPool.invalidate()
- **Phase 5: 测试验证**
  - 创建 `tests/test_agent_loop.py`：13 个测试全部通过
    - Hook 系统：注册、触发、拦截、权限检查
    - Tool Pool：单例、失效、统计
    - agent_loop：停止条件、工具执行、处理器注册
    - 集成测试：V2 开关默认关闭、settings 字段存在
  - 容器验证：`docker restart ai-agent-api` → API 正常启动
- **开关状态**：
  - `USE_AGENT_V2=false`（默认关闭，不影响现有行为）
  - `ENABLE_KNOWLEDGE_TOOL=true`（V2 开启时知识库工具可用）
- **架构图**：`designs/plan-chat-architecture-refactor.md`（含 Hook 时序图、Tool Pool 初始化流程）
- **启动脚本更新**（`docker/start.bat`）：
  - 支持 `--v2` 参数启动 V2 架构
  - `docker/.env` 添加 `USE_AGENT_V2=false` 配置项
  - `docker-compose.yml` 注入 `USE_AGENT_V2` 环境变量到 api 容器
  - 用法：`start.bat` (V1 默认) 或 `start.bat --v2` (V2 架构)
- **两套架构并存**（用户决定保留）：
  - V1：`app/agent.py`（无条件 RAG + Intent Router）
  - V2：`app/agent_v2.py`（Model-Driven + Hooks + Tool Pool）
  - 通过 `should_use_v2_architecture()` 判断用哪个

## TapNow Canvas V2 UI 设计图（晚间）
- **v2 文件**: `tapnow-canvas-v2.html` — 基于**真实 API token 登录抓取**的像素级还原
- **数据来源**: 用户提供了 `app.tapnow.ai` 的 Bearer JWT → 用 node fetch 直接调用 canvas/模板/community 等 API（200 成功）→ Playwright 注入 token 到 localStorage+cookie → 成功绕过登录墙加载画布编辑器和工作空间主页
- **真实发现**: ①画布编辑器用 **React Flow** 做节点引擎 ②左侧工具栏7按钮(➕🔍📁⊞💬🕐👤) ③右侧底部Agent输入区(`描述创意或需求/@添加画布内容`) ④模型选择 Kimi 2.6 ⑤Tapies 积分(200) ⑥工作空间有项目列表+Agent建议+精选社区流 ⑦社区页(TapTV)有视频创作过程展示 ⑧促销弹窗(Creative OS 订阅)
- **v1 vs v2 差异**: v1 是基于官网/评测猜测的结构；v2 全部来自真实截图+API+DOM 提取。顶栏布局、左工具栏按钮顺序、右侧 Agent 输入区位置、底部缩放控件、节点库抽屉、页面导航结构全部修正。
- **验证**: Playwright 8项全通过(7节点/7连线/12库项/选中/3页面切换/缩放/拖拽/0报错)
## TapNow Canvas V3 全交互层补全（晚间续）
- **v3 文件**: `tapnow-canvas-v3.html` — 在 v2 基础上系统性重建全部交互逻辑
- **新增交互（21 项 Playwright 验证全通过，0 报错）**：
  - **节点操作**: 从节点库/添加弹窗真正新增节点(12 种类型)、Delete 键删除、选中节点显示 ✕ 删除按钮、右键菜单(复制/添加并连接/删除)、双击标题重命名(contentEditable)、Shift+点击多选、Ctrl+A 全选
  - **连线**: 从节点端口拖拽生成贝塞尔虚线预览 → 松手到目标节点自动创建连线(选端口)、点击连线删除
  - **画布**: 拖拽空白平移(pan)、滚轮缩放(对准光标坐标)、Shift+拖拽框选多选、fitView 适配视图、网格开关、小地图(节点点+视口矩形)
  - **属性检查器**: 选中节点后实时显示编辑器(标题输入/提示词textarea/模型下拉/尺寸参数/生成按钮)，编辑同步回节点 DOM；多选时显示批量删除；未选时骨架屏；属性/对话 Tab 切换
  - **Agent 输入条**: 模型下拉(Kimi/GPT/Claude/Kling等)可切换、回车发送生成对话气泡+Agent 回复(含 typing 动画)、技能 chip 点击注入文本(@引用)
  - **工具栏面板**: 搜索面板(过滤节点/资源)、评论面板(用户+Agent对话)、历史面板(操作时间线)、文件面板(素材列表)、用户菜单(个人资料/订阅/设置/退出)、添加节点弹出(12 项)
  - **工作空间页**: Tab 筛选(所有项目/精选推荐/最近打开)实际隐藏/显示卡片、精选横滑箭头(scrollBy)、ws Agent 输入发送反馈、skill chip 点击反馈
  - **社区 TapTV 页**: 卡片点击打开详情弹窗(视频预览区+播放按钮+创作过程步骤列表+作者信息)
  - **全局**: Toast 提示系统、ESC 关闭弹窗、竞技场按钮反馈、分享/通知按钮反馈、促销弹窗开关
- **技术实现**: 状态驱动渲染(state.nodes/state.edges/state.sel/state.zoom/state.pan)、viewport transform 统一管理 pan+zoom、portOffset 函数计算端口坐标(4 方向)、toViewport 屏幕转画布坐标、elementFromPoint 连线命中检测
- **修复**: 全局 mousedown closePopups() 会先于 popover item click 执行导致弹出项点击失效 → 加 closest 守卫; libDrawer fixed 定位跨页面遮挡 → switchPage 自动关闭

## TapNow 全产品 UI 还原 + PRD（用户授权用其 JWT token 登录抓取）
- 基于用户提供的 Bearer JWT（wah1763751448@163.com 账号）真实登录抓取 `app.tapnow.ai`：画布编辑器(React Flow)/工作空间主页/社区 TapTV 三页 DOM + `/api/*` 响应。
- 交付物：`tapnow-canvas-v2.html`(像素级还原3页) → `tapnow-canvas-v3.html`(21项交互全补全, Playwright 21/21 通过)。
- 交付物：`tapnow-PRD.md`(详细 PRD, 77 条 F-* 需求, 含画布/节点库12类/属性检查器/Agent条/工作空间/社区/导航/全局面板, 逐功能对齐线上)。
- 产品真实结构事实：顶栏(Canvas名+修改时间|Tapies 200|社区|分享|🔔|W头像|✏️)；左工具栏(➕🔍📁⊞💬🕐👤)；底部 Agent 条(Kimi 2.6 + 技能chip)；底部缩放栏+小地图；导航(主页/工作空间/TapTV/竞技场/价格方案/免费体验)；节点12类分4组(输入/生成/结构/智能)；社区TapTV视频卡(作者@/观看量/时长/查看创作过程)。
