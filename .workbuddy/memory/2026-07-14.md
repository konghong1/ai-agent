# 2026-07-14

## 电商套图 · 提示词工程重构（按用户流程诉求）

用户要求把流程定型为：用户输入(核心卖点+市场配置) → 全局配置(模型+出图规划个性配置) →
AI 生成合适风格提示词 → **记录提示词输入/输出** → 分解(解析) → 生成图片；并强调「AI 调用那步
不要自己写提示词，只告诉模型用户配了啥、想产出啥类型」。

### 本次落地
- `app/gallery_prompt_ai.py`
  - `build_user_config_text` 新增【市场配置】段（market_config：目标市场/平台/人群/品类/季节/价格带），
    原已含核心卖点/出图规划个性配置/通用设置/类型/比例；强化「你的任务=基于配置+参考图产出该类型」表述。
  - `generate_prompt_via_ai` 新增返回 `prompt_input`（喂给模型的完整意图描述）与 `prompt_raw`
    （模型原始返回文本）；模板降级时给明确占位说明而非空值。
- `app/models.py`：GalleryRecord 新增 `prompt_input` / `prompt_raw` 列。
- `app/core/database.py`：迁移函数新增这两个列的 ALTER（用 inspect 防重复，SQLite/MySQL 通用）。
- `app/schemas.py`：GalleryRecordRead 增加 `prompt_input` / `prompt_raw`。
- `app/gallery_service.py`：`run_gallery_task` 建记录时写入这两个字段。
- 前端 `web/src/services/gallery.ts`：GalleryRecord 接口加 `prompt_input?/prompt_raw?`。
- 前端 `web/src/pages/EcommerceGallery/index.tsx`：PromptBadge 弹窗加「提示词溯源」折叠区
  （①喂给 AI 的输入 ②AI 原始返回），两处调用点已传参。
- `web/src/pages/EcommerceGallery/gallery.css`：prompt-trace 相关样式。

### 验证
- `python -m py_compile` 全过。
- `tests/test_gallery_prompt_ai.py` 9 passed（新增 market_config 入输入、prompt_input/prompt_raw 留痕、
  降级占位 3 个用例）；1 个真实 Agnes 调用因沙箱无 OPENAI_API_KEY 跳过（预期）。
- 前端 `tsc --noEmit` / `vite build` **未能在本地跑**：本工作区 `web/` 未装 typescript/vite
  （工具链在 Docker 构建环境）。改动为可选 prop + 折叠块 + CSS，类型安全、风险低；
  上线前需在 Docker 跑 `cd web && npm install && npm run build`。

### 注意
- 真实出图用 Docker 栈（MySQL + 5 图片模型）；本地 uvicorn+SQLite 无模型走离线降级。
- 改后端后需重启 `ai-agent-api`+`ai-agent-worker`；前端 Ctrl+F5。

## 🔥 排错：项目数据全空 = ai-agent-api 崩溃重启（MySQL TEXT 默认值坑）

现象：浏览器能开（web:80 返回 200）但项目列表/数据全空。
根因：`docker ps` 显示 `ai-agent-api` 处于 `Restarting (3)` 状态；日志报
`(pymysql.err.OperationalError) (1101, "BLOB, TEXT, GEOMETRY or JSON column 'prompt_input' can't have a default value")`
`[SQL: ALTER TABLE gallery_records ADD COLUMN prompt_input TEXT DEFAULT '']`。
即上一轮加的 DB 迁移给 TEXT 列写了 `DEFAULT ''`，MySQL 8 拒绝 → 每次启动跑迁移即崩 → restart loop。

### 修复（两处）
- `app/core/database.py` 迁移：prompt_input/prompt_raw 的 ALTER 去掉 `DEFAULT ''`（TEXT 列允许 NULL）；prompt_en 同改。
- `app/models.py` 509/511 行：`mapped_column(Text, default="", server_default="")` → 去掉 `server_default=""`（MySQL create_all 新生库也会炸）。

### 生效
- api 容器 `volumes: - ../:/app` 是 bind mount，无需 rebuild 镜像，直接 `docker restart ai-agent-api`。
- 重启后日志：`Added prompt_input/prompt_raw column` → `Application startup complete` → uvicorn running。
- 验证：`curl /api/gallery/projects` 从 `000 不可达` 变 `401`（=服务活着、要求登录），链路通。
- 教训已写入 MEMORY.md「MySQL 迁移 TEXT 列禁带默认值」。

### 用户下一步
- 浏览器 Ctrl+F5 刷新 → 重新登录 → 项目数据恢复（schema 字段已声明可空，旧记录 NULL 不 500）。
- 之前加的「提示词溯源」前端 UI 仍未 build（web/ 无本地工具链），要看溯源面板需 Docker 里 `cd web && npm install && npm run build`。

## 二次排错：提示词还是模板拼装的，AI 路径没走

现象：用户重新生成图片后，提示词弹窗内容仍是“精修，高锐度，无镜头畸变... 正向增强与约束... 负面提示词”的模板腔；数据库记录 `prompt_source='template'`，`prompt_input=0, prompt_raw=0`。
根因：Docker 环境只注入 `AGNES_API_KEY`/`AGNES_BASE_URL`，但 `gallery_prompt_ai.py` 的 `generate_prompt_via_ai` 与 `_build_client` 检查的是 `settings.openai_api_key`（对应 `OPENAI_API_KEY`）和 `settings.openai_base_url`（对应 `OPENAI_BASE_URL`）。key 为空 → 立即抛异常 → 降级到模板引擎，根本没有调大模型。`openai_model` 默认 `gpt-4o-mini` 也会误用。

### 修复
- `app/gallery_prompt_ai.py` 增加 `import os`。
- 新增兼容函数 `_get_ai_key()` / `_get_ai_base_url()` / `_get_ai_model()`：
  - key 优先 `OPENAI_API_KEY`，其次 `AGNES_API_KEY`；
  - base_url 优先 `OPENAI_BASE_URL`，其次 `AGNES_BASE_URL`；
  - model 优先 `AGNES_MODEL`，其次非默认的 `OPENAI_MODEL`，兜底 `agnes-2.0-flash`（避免默认 `gpt-4o-mini` 误用）。
- 替换 `generate_prompt_via_ai` / `ai_write_selling_points` / `ai_write_type_config` 里的 key 检查，以及 `_build_client` / `_chat_multimodal` 的 client 初始化与 model 选择。
- 更新模块 docstring，说明环境变量兼容关系。

### 验证
- `python -m py_compile app/gallery_prompt_ai.py` 通过。
- `tests/test_gallery_prompt_ai.py` 9 passed / 1 skipped（真实 Agnes 调用跳过）。
- 重启 `ai-agent-api` 后，容器内验证：`_get_ai_key()` 有值、`base_url=https://apihub.agnes-ai.com/v1`、`model=agnes-2.0-flash`。

### 用户下一步
- 重新生成一次图片，即可得到 AI 生成的提示词（不再是模板）。
- 当前前端弹窗还是旧 build，不会显示「AI」徽标和「提示词溯源」折叠区；要看输入/输出留痕，需在 Docker 里重新 build 前端。

## 三次排错：AI 路径仍降级 = agnes-2.0-flash 是推理模型，max_tokens 太小把答案额度吃光

现象：第 2 次修复（兼容 AGNES_API_KEY）后，重新生成的记录仍 `prompt_source='template'`、`prompt_input`=真实配置、`prompt_raw=0`；api 日志 `POST apihub.agnes-ai.com ... 200 OK` 但 `AI 返回结构异常，降级模板引擎：''`。
根因（容器内真实调 agnES 抓包定位）：`agnes-2.0-flash` 是**带思维链的推理模型**——思考写进 `reasoning_content`、答案写进 `content`。原 `_chat_multimodal` 把 `max_tokens` 写死成 `1200`，思考过程占满额度后 `content` 为空串（`finish_reason:"length"`），代码读到空 → 降级。GET /models 确认 5 个模型：agnes-1.5-flash / 2.0-flash / image-2.0/2.1-flash / video-v2.0。

### 修复
- `AI_PROMPT_TIMEOUT` 90→120；新增 `AI_PROMPT_MAX_TOKENS=4096`（实测 `2048` 仍空、`4096` 稳定 `finish=stop` 且 content 有值）。
- `_chat_multimodal` 加 `max_tokens` 形参（默认 4096），不再写死 1200。
- `generate_prompt_via_ai` 改为重试（最多 3 次）：content 空/解析失败 → 上调 `max_tokens`(×2, 上限 8192) 重试；**成功或降级都保留 `prompt_input`(真实配置) 与 `prompt_raw`(最后一次原始返回)**，满足用户“把喂给 AI 的提示词和 AI 原始返回展示给我看”的诉求。
- 测试 `test_fallback_keeps_trace_placeholder` 对齐新行为（注入假 key 走真实降级留痕路径）。

### 验证（铁证）
- 容器内真实调用 `generate_prompt_via_ai`（配置=亚马逊主图/45度俯角/全球/汉服）：`prompt_source=ai`、`prompt_input` 长度 358、`prompt_raw` 长度 1079；`prompt_cn` 为模型写的差异化中文提示词（非模板结构），`prompt_en` 纯英文。
- 溯源报告落盘 `ai/agent-output/verify-shots/prompt_ai_trace.md`。
- `py_compile` 全过；`tests/test_gallery_prompt_ai.py` 全绿（9 passed / 1 skip）。

### 结论
用户此前的截图像素级模板腔，根因是 ①AGNES key 不兼容（已修）+ ②本坑（推理模型 max_tokens 太小）→ 连续降级到旧 `gallery_prompt.build_prompt_bilingual`。现在 AI 路径实测可用，用户重新生成即可看到 AI 写的提示词；前端「提示词溯源」面板需 Docker build 后才可见。

## 五次重构：提示词精简工程（按用户最新诉求）

用户明确四点：①只有下拉选择的推荐类型才走 AI 改写；②用户核心卖点只读、原则上不改写；
③出图规划选「自定义子任务」则**不走 AI 改写**，原样透传；④AI 改写严格按 8 维结构
`[[主体]+[细节修饰]+[场景环境]+[构图视角]+[光影质感]+[画质技术参数]+[风格参考]+[负面约束]]`，
可按配置侧重点不同；示例要求短小精炼（给了“电商首图核心展示，主体为女童...”样例）。
并指出旧模板 `_assemble` 也很长、效果不好，需一并优化。

### 落地（4 处）
- `app/gallery_service.py` `_build_prompt` 加路由：`get_plan_type(item.type_id).custom==True`
  → 直接 `_build_custom_prompt(item)`，用 `personal_settings["自定义需求"]` 或 `note` 原文，
  `prompt_source="custom"`，**不调 AI、不翻译、不改写**；其余推荐类型走 `generate_prompt_via_ai`。
- `app/gallery_prompt_ai.py` `_PROMPT_SYSTEM` 重写：核心卖点标注「参考，不可修改」；
  要求按 8 维组织（只写相关维度）、短小精炼、像真人视觉指导；附用户示例风格。
- `app/gallery_prompt.py` `_assemble`（中文模板兜底）精简：去掉 8 个【分桶标题】与
  `【正向增强与约束】/【负面提示词】`两块标题，改为**连续流动段落**；去掉 `_subject_fidelity_block`
  的「【主体一致性+颜色锁定】」【】前缀；正向增强块压缩（保留零/允许文字 Linter 锚点短语）、
  负面约束压缩为一行。桶数据收集逻辑（配置差异化、参考图保真、颜色锁定、无人物、palette、
  angle collage）全保留 → 测试 22 项断言不受影响。
- 测试：`tests/test_gallery_prompt_ai.py` 新增 `test_custom_subtask_uses_original_text`（自定义子任务
  source=custom、用原文、不调 AI）；`tests/test_gallery_prompt.py` 22 项全绿（断言绑定数据路由/helper
  短语，不依赖分桶标题）。

### 验证（铁证）
- 本地：`py_compile` 全过；`test_gallery_prompt.py` 22 passed；`test_gallery_prompt_ai.py` 10 passed / 1 skip。
- 容器内真实 AGNES 调用（优化后 8 维 `_PROMPT_SYSTEM`，amz 类型）：
  `prompt_source=ai`、`prompt_cn` 仅 **158 字**，内容为
  “亚马逊合规主图，女童汉服套装严格匹配参考图版型与配色，45度俯角居中展示，四周均匀留白于纯白
  背景RGB 255 255 255。棚拍柔光均匀覆盖服装...高分辨率边缘锐利，1:1画幅，无文字水印...” ——
  完全贴合用户示例的短小精炼风格，非模板。
- 模板兜底（无 AGNES key 时）：amz 中文 1328 字、连续流动、无标题，比旧版 ~2000+ 字精简且去模板腔。
- 已 `docker restart ai-agent-api` 生效（bind mount，无需 rebuild）。

### 用户下一步
- 重新生成图片：推荐类型走 AI 出 158 字级精炼提示词；自定义子任务原样透传。
- 前端「提示词溯源」面板（输入/输出留痕）仍需 Docker `cd web && npm install && npm run build` 后才可见。

## 收尾验证（502 恢复后二次确认）

502 网络中断恢复后，重新走读确认上述 4 处改动均在位、无遗漏：
- `gallery_service.py`：`_build_prompt` 路由 + `_build_custom_prompt`（`prompt_source="custom"`）在位。
- `gallery_prompt_ai.py`：`_PROMPT_SYSTEM` 8 维 + `AI_PROMPT_MAX_TOKENS=4096` + 重试 ×2 上限 8192 在位。
- `gallery_prompt.py`：`_assemble`(中文) / `_assemble_en`(英文) 均为连续流动、无分桶标题；残留
  `【正向增强与约束】/【负面提示词】` 仅出现在 docstring/注释，不进输出 → 顺手把 `_assemble` 的
  V12 docstring 对齐成 V13 连续流动描述，避免误导团队。
- `tests/test_gallery_prompt_ai.py`：`test_custom_subtask_uses_original_text` 在位。

### 验证
- `.venv` 无 pytest；用系统 `Python311` 跑：`tests/test_gallery_prompt.py` + `tests/test_gallery_prompt_ai.py`
  **33 passed**（22 模板 + 10 AI + 1 自定义子任务）in 0.66s。
- `py_compile` 三文件全过。
- 结论：五次重构（含 4 处代码 + 模板精简 + 验证测试）全部完成、当前环境可跑、全绿。无未完成任务。

## 规格参数图升级（用户截图诉求）

用户希望「规格参数图」类型能生成带测量标注 + 尺码表 + 人体剪影的图（如女童汉服尺码表示例），
而不是只出衣服主体、没有参数尺码。

### 落地
- `app/gallery_config.py`：
  - `TYPE_PERSONAL["spec"]` 新增字段 `规格参数原文`（用户可粘贴真实尺码/参数数据）。
  - `COPY_ALLOWED_TYPES` 加入 `"spec"`，允许规格参数图在画面呈现文字。
- `app/gallery_prompt_ai.py`：
  - `_PROMPT_SYSTEM` 新增「规格参数图」特殊规则：服饰类必须含测量箭头/中文标注、尺码表、人体剪影；
    用户提供了 `规格参数原文` 则必须严格填入表格，不得编造；明确 `prompt_en` 允许保留中文标签/表头/数据。
  - `_strip_cjk` 加 `type_id` 参数，对 `spec` 类型保留中文（其余类型仍零中文）。
  - `build_user_config_text` 对 `spec` 单独输出「规格参数图·数据强制」段，把用户数据强调查给模型。
- `app/gallery_prompt.py`：
  - `_decide_copy_policy` 对 `spec` 强制 `copy_language=中文`。
  - `_assemble`（中文模板兜底）新增 `spec` 专用块：左侧产品主体 + 测量箭头标注（衣长/裙长/袖长/胸围）、
    右侧尺码表、人体剪影、真实数据回填。
  - `_assemble_en`（英文模板兜底）同样新增 `spec` 块，并保留中文标签/表头/数据。
- `web/src/pages/EcommerceGallery/TypeSettingsModal.tsx`：
  - `规格参数原文` 字段渲染为 `Input.TextArea`（3 行），方便粘贴多行尺码数据。
  - 需要 Docker 里 `cd web && npm install && npm run build` 才生效。

### 验证
- `py_compile` 五文件全过。
- `pytest tests/test_gallery_prompt.py + tests/test_gallery_prompt_ai.py` **38 passed**（新增 3 个模板 spec 测试 + 2 个 AI spec 测试）。
- 模板兜底输出示例：中文 prompt 含「尺码表」「衣长/裙长/袖长/胸围」「身高/年龄段」「人体剪影」、
  英文 prompt 含对应中文标签；`110码 衣长62...` 真实数据落地。
- `docker restart ai-agent-api` 生效，日志 `Application startup complete`。

### 用户下一步
- 在「规格参数图」属性设置里填写 `规格参数原文`（如多行尺码数据），然后重新生成。
- 前端 textarea 输入框需 Docker build 后才可见；当前可用单行输入框先粘贴数据。

## 聊天视频生成"生成不出来"排查 + 修复（12:23 轮）

用户现象：聊天里只输入提示词、没选参考图，视频生成不出来。

### 排查结论（关键：与"有无参考图"无关）
- 纯文本 t2v（无参考图）实测能提交且 Agnes `agnes-video-v2.0` 接受并 completed（progress=100）——缺图不是报错原因。
- 历史 2 条视频消息（MSG 12/14）均 `status=completed` + `reference_images=[]`，但 **blocks 里根本没有 `video_url` 字段**。
- 根因：Agnes 视频完成响应里，**真实播放 URL 在 `metadata.url`**（`https://platform-outputs.agnes-ai.space/videos/...mp4`），而
  `MediaService.get_video_status` 的 URL 提取只看顶层 `video_url/output/url/remixed_from_video_id`，**完全没读 `metadata.url`**
  → `video_url` 永远为空 → 前端 `MediaCard` 拿不到地址 → 视频放不出来（用户感知"生成不出来"）。
- 已排除：nginx SSE（`proxy_buffering off` + `proxy_read_timeout 86400s` 正确）、前端 watchVideo（正确传 provider_id + 解析 completed）、SSE 鉴权（`?token=` 兼容）、前端发送按钮 disabled 逻辑（不校验参考图）。

### 修复（`app/media.py` 的 `get_video_status`）
- 第一处（顶层 video_url 归一化）：候选列表改为字段**值** `[data.get("remixed_from_video_id"), data.get("output"), data.get("url"), (metadata or {}).get("url")]`。
- 第二处（raw_url 提取进 MinIO 存储）：增加 `(data.get("metadata") or {}).get("url")`。
- 效果：提取 `metadata.url`(Agnes CDN) → 下载存储到 MinIO → `video_url` 变成内部代理 `/api/media/assets/by-key/videos/2/...mp4`，前端稳定播放。

### 验证
- `py_compile app/media.py` 通过。
- 容器内实测已完成任务：`video_url = /api/media/assets/by-key/videos/2/2026/07/14/1d83df...mp4`（下载存储成功），`object_key` 非空。
- `docker restart ai-agent-api` 生效（加载新代码）。
- 一次性回填脚本：对 DB 中所有 `status=completed` 但缺 `video_url` 的视频消息重新提取+存储，MSG 12/14 均 FIXED（旧 CDN 链接未过期，下载成功）。
- 注意：本次无新增单测（纯属响应字段解析 bug，非逻辑分支），但已用真实 Agnes 任务端到端验证提取+存储全链路。

### 用户下一步
- 聊天里重新生成视频（无论有无参考图）现在都会正确拿到可播放地址。
- 历史已完成的视频消息已回填 URL，刷新聊天即可播放。

## 出图规划「补充说明」未传入 AI + 提示词展示范围收窄（12:55 轮）

用户两个诉求：
1. 出图规划列表里某项的「补充说明」填写后，没传给 AI 生成提示词。
2. 提示词只在「任务列表」查看，详情弹窗和发布弹窗不需要。

### 根因
- `app/gallery_prompt_ai.py` 的 `build_user_config_text`（喂给 AI 的用户意图文本）读了卖点/市场配置/个性化配置/通用设置/规格数据，但**完全没读 `item.note`（补充说明）**。
- 前端 `PromptBadge` 在两处渲染：任务列表(`index.tsx:1162`) + 作品详情弹窗(原 1355-1359)；发布弹窗本就没有。

### 修复
- `gallery_prompt_ai.py:214-217`：在【核心卖点】后补 `【补充说明】{note}`（取 `item.note`）。`generate_prompt_via_ai` 的 `prompt_input` 即该文本，故溯源面板自然含补充说明。
- `gallery_prompt.py:534-537`（`_assemble` 中文）：`note` 非空时注入 `补充说明：{note}`。
- `gallery_prompt.py:890-893`（`_assemble_en` 英文）：**仅规格参数图(spec)注入**（英文生成版对非规格类型执行零中文硬约束，避免泄漏中文用户内容）；spec 允许中文故注入 `supplementary note: {note}`。
- 前端 `index.tsx`：移除详情弹窗(原 1355-1359)的 `PromptBadge`，只保留任务列表 1162。

### 验证
- `py_compile` 四个文件全过。
- `pytest` **40 passed**（原 38 + 新增 `test_note_flows_into_ai_input` / `test_note_in_template_prompt`）。
- `docker restart ai-agent-api` 生效。
- 注意：前端改动需 Docker `cd web && npm install && npm run build` 才可见（移除详情弹窗提示词徽标）；后端 AI 输入/模板兜底已即时生效。

### 用户下一步
- 在出图规划项填写「补充说明」，重新生成 → AI 路径与模板兜底都会带上该内容。
- 若要看前端详情弹窗不再显示提示词，需执行 web 前端构建。

## 规格参数图中文乱码修复：方案 A（纯视觉图 + 后端文字叠加）（13:23 轮）

用户贴出规格参数图，尺码表/测量标注全成乱码——根因是扩散模型（Agnes/SD）对汉字字形渲染极弱。
用户从 5 个方案中选定**方案 A**：图像模型只生成干净无文字视觉，所有中文由后端用真实 CJK 字体精确叠加。

### 实施
- 新增 `app/spec_overlay.py`：
  - `resolve_spec_font()`：优先捆绑 `app/assets/fonts/simhei.ttf`（从宿主 `C:/Windows/Fonts/simhei.ttf` 拷入，容器 bind mount 可读），系统字体回退，最终回退 PIL 默认。
  - `parse_spec_data(text)`：按 `；/；/\n` 分行，识别尺码（`110码`/`S/M/L`/`110` 数字开头），正则抽取 `名称+数值[单位]` 为表格列；返回 `{headers, rows}`。
  - `overlay_spec(result_path, spec_text, note, title, category)`：合成「左侧产品(cover 裁剪到 60% 宽) + 右侧浅灰面板(标题/尺码表/补充说明) + 左侧半透明深色尺寸图例」，存为新 `results/xxx.png` 返回相对名。另提供内存版 `overlay_spec_image` 供测试/预览。
- `app/gallery_prompt_ai.py`：
  - `_PROMPT_SYSTEM` 第 6 条 spec 规则改为「画面严禁任何文字，产品居左、右侧预留空白面板、可含极简测量引导线/人体剪影，数据不进图像模型」。
  - `build_user_config_text` 的 spec 分支改为告知模型「纯视觉+后端叠加、严禁绘制文字」，数据作为叠加层输入（不进图像模型）。
  - `_strip_cjk` **去掉 spec 例外**（spec 的 prompt_en 也必须纯英文，杜绝中文泄漏到图像模型）。
- `app/gallery_prompt.py`：
  - `_decide_copy_policy` 的 spec 改 `allow_text=False`（画面零文字）。
  - `_assemble` / `_assemble_en` 的 spec 段改为无文字视觉指令；note 注入对 spec 跳过（补充说明改由叠加层渲染）。
- `app/gallery_service.py` `run_gallery_task`：spec 类型且 `real` 有 filename 时调用 `overlay_spec`，更新 `rec.result_filename/result_url`。

### 验证
- `py_compile` 全过；`pytest` **41 passed**（原 40 + 新增 `test_spec_overlay_parse_and_render`；同步改写 spec 旧断言：数据进 AI 输入/渲染策略、prompt_en 零中文、模板零文字、strip_cjk 去 spec 例外）。
- 容器内实测 `resolve_spec_font`/`parse_spec_data`/`overlay_spec_image` 均正常（FreeTypeFont，数据解析正确）。
- 本地生成演示图 `ai/agent-output/verify-shots/spec_overlay_demo.png`（尺码表+尺寸图例+补充说明，中文清晰无乱码）。
- `docker restart ai-agent-api` 已生效。

### 关键点 / 坑
- 字体是方案 A 的前提：容器原本**零 CJK 字体**，必须捆绑。simhei 是 Windows 专有字体，生产分发建议换成 OFL 许可的 Noto Sans SC（已在代码 `_FONT_CANDIDATES` 中优先尝试 NotoSansCJK 路径，将来装了即可自动切换）。
- 之前 spec 在 `_assemble_en` 残留中文测量标签（`衣长/裙长...`）会泄漏进英文提示词 → 已改为纯英文 `length/chest/sleeve/waist`。
- 前端「规格参数原文」输入框仍建议 Docker build 后变 textarea（见上轮），不影响本次修复。

### 用户下一步

## 规格参数图尺寸标注线强化（14:11 轮）

用户反馈：方案 A 只是不让模型写中文，结果连测量尺寸线也不画了。需要让最终图有清晰的尺寸标注线。

### 修复
- 明确问题：方案 A 提示词规则里「画面中绝对不能出现任何文字、数字、字母、表格、标注」的「标注」把测量引导线也一并禁了；后端叠加层只画了一个深色小文字卡，不是尺寸线。
- `app/gallery_prompt_ai.py` / `app/gallery_prompt.py`：spec 规则改为「模型只出干净产品图（右侧留白），不要画任何测量线/箭头/数值；尺寸标注线由后端统一精确叠加」。
- `app/spec_overlay.py`：新增 `_arrow_tips`/`_dim_value`/`_draw_measurement_lines`：
  - 自动识别服饰/非服饰，用双向箭头画尺寸线（衣长/胸围/肩宽/袖长 或 高度/宽度/厚度）。
  - 从规格数据首行取数值，标签显示中文名 + 数值（如「衣长 62」），由真实 CJK 字体绘制，不乱码。
  - 移除旧版深色小文字卡，避免与模型视觉冲突。
- 验证：`pytest` 41 passed；生成 `ai/agent-output/verify-shots/spec_overlay_v2_demo.png` 与 `_gadget.png` 确认效果。

### 待决策
- AI 提示词批量生成方案：用户希望一次生成多个提示词。设计了 A/B/C/D 四种方案，待用户选择后实施。

## 批量提示词生成策略模式（14:33 轮后实施）

用户选择：默认方案 A（单次批量 AI 调用），但要求做成策略模式；若效果不佳可切到方案 B（并发并行调用）。

### 方案 A（默认）
- `app/gallery_prompt_ai.py`：
  - 新增 `_get_batch_mode()`：读 `AI_PROMPT_BATCH_MODE`（默认 1，设 2 切方案 B）。
  - 新增 `_PROMPT_BATCH_SYSTEM`：一次性给 AI「产品图片 + 公共参数 + 每个出图方向侧重点/参数/比例」，让 AI 从整体视角批量生成各方向提示词，返回 JSON 数组。
  - 新增 `build_batch_user_config_text()`：组装公共上下文 + 各 item 侧重点。
  - 新增 `generate_prompts_batch_mode_1()`：单次 AI 调用，返回 `{item.id: {prompt, prompt_en, prompt_source, prompt_input, prompt_raw}}`；按 `1024 token/项` 估算 max_tokens（上限 8192），失败自动重试，失败返回已解析部分；兜底走单条 `_build_prompt`。
  - 新增 `_extract_json_array()`：兼容 ` ```json` 代码块，提取 JSON 数组。

### 方案 B（可切换）
- `app/gallery_prompt_ai.py`：`_get_batch_mode()` 返回 2 时启用。
- `app/gallery_service.py`：
  - 重构 `run_gallery_task`，先「阶段 0 收集 items_meta」→「阶段 1 批量生成提示词 `_build_prompts_for_plan`」→ 再逐张生成。
  - `_build_prompts_for_plan()`：策略分发入口。`custom=True` 自定义子任务原样透传；非自定义项按 batch mode 分发方案 1/2；缺失项兜底串行 `_build_prompt`。
  - `_build_prompts_parallel()`：方案 B，`ThreadPoolExecutor(max_workers=4)` 并发并行调用 `_build_prompt`，单条失败串行兜底。

### 测试
- `tests/test_gallery_prompt_ai.py` 新增 4 个用例：`test_extract_json_array`、`test_batch_prompt_mode_1`、`test_build_prompts_for_plan_custom`、`test_build_prompts_for_plan_mode_2`。
- 验证：`pytest tests/test_gallery_prompt.py + tests/test_gallery_prompt_ai.py` **45 passed**（此前 41 + 4 新增）。
- `py_compile`：4 个核心文件（`gallery_prompt_ai.py`、`gallery_prompt.py`、`gallery_service.py`、`spec_overlay.py`）全部通过。

### 使用方式
- 默认方案 A 已生效；如需切方案 B，设环境变量 `AI_PROMPT_BATCH_MODE=2`（Docker 可在 `docker-compose.yml` 加 `AI_PROMPT_BATCH_MODE=2` 后 `docker restart ai-agent-api`）。
- 建议先实际跑一套多图生成，验证方案 A 质量；若效果不理想再切方案 B。

### 完成状态
- 两项用户反馈（规格图尺寸线 + 批量提示词）全部完成、测试全绿。Docker 后端已生效，前端无需改动。

## 提示词质量强化（15:41 轮）：8 维强制 + 单图多视角/多场景逐格

用户反馈：AI 生成的提示词仍有瑕疵、很多不满意；要求按
`[主体]+[细节修饰]+[场景环境]+[构图视角]+[光影质感]+[画质技术参数]+[风格参考]+[负面约束]`
生成，特别是多视角/多场景图要让 AI 在单图内根据描述生成不同场景与细节（含九宫格/左右构图等），都更细节。

### 改动
- `app/gallery_prompt_ai.py`：
  - `_PROMPT_SYSTEM` / `_PROMPT_BATCH_SYSTEM` 重写：强制 8 维全部覆盖且写细（不再"省略无关维度"）；
    新增「单图多视角/多场景」版式规则——先定网格（2×2 四宫格 / 3×3 九宫格 / 左右分屏 / 上下分屏 / 田字格 / 主图+辅图环绕），
    再对每一格逐一写【主体(该视角/状态)+场景环境+光影质感+构图视角+细节修饰】，跨格产品一致、格间细线/留白分隔、整图光影统一。
  - 新增 `_detect_multi_cell(item)`：扫描 `type_id==angle` 及 personal/common 值中的 拼接/拼贴/宫格/分屏/多场景/多视角/多角度/九宫格/四宫格/左右对比/上下对比/产品主体+多场景 等关键词，
    返回「多视角拼贴」或「多场景拼接」中文版式指令；注入 `build_user_config_text`（单图）与 `build_batch_user_config_text`（每项）。
  - `AI_PROMPT_MAX_TOKENS` 4096 → 6144（更细的逐格提示词留额度，重试仍升到 8192）。
- `app/gallery_prompt.py`（模板兜底同步）：
  - 新增 `_detect_multi_cell_type(cfg)`（与 AI 口径一致）。
  - `_assemble`(中) / `_assemble_en`(英) 在检测到单图多格版式时追加逐格场景/角度/光影描述指令，降级路径也与 AI 路径一致。

### 测试
- 新增：`test_detect_multi_cell`、`test_build_user_config_text_multi_cell`（AI 路径）、`test_assemble_multi_cell_angle`、`test_assemble_multi_cell_scene`（模板路径）。
- 验证：`pytest tests/test_gallery_prompt.py + tests/test_gallery_prompt_ai.py` **49 passed**（45 + 4 新增）；4 文件 `py_compile` 通过。
- 坑：测试断言大小写/措辞要与实际注入文本一致（英文注入串改短为 "multi-angle/multi-scene collage, each panel..."，中文为"每一个格子"）。

### 下一步
- `docker restart ai-agent-api` 使后端改动生效（bind mount 免 rebuild）。
- 实测一次多视角/多场景生成，确认逐格描述符合预期；如仍偏模板腔，可再收紧系统提示词或调高温度。

## 「一键做同款」发布丢参数 + 创作案例假数据（16:02 轮）

用户诉求：①「一键做同款」点「发布到创作案例」时，没把用户设置的生成参数带上；
②创作案例里展示的还是假数据；要求修复并验证。

### 根因定位
1. 假数据：`gallery_showcases` 表 6 条全是 `seed_showcases` 注入的 SVG 占位图（`/api/gallery/files/showcase/*.svg`，由 `gallery_config.SHOWCASE_SEED` 定义），与用户真实成图无关。
2. 参数丢失（三处叠加）：
   - `models.GalleryShowcase` 无参数字段；
   - `gallery_service.publish_showcase` 只存图不存参；
   - 前端创作案例「生成同款」按钮原是 `openDrawer()` 空壳（行 1235/1465/详情弹窗），不回填任何配置。

### 修复
**后端**
- `app/models.py`：`GalleryShowcase` 新增 `payload`（JSON，可空、默认 {}），结构 `{plan_items:[plan_item_snapshot...], market_config, output_config, selling_points}`。
- `app/gallery_service.py`：`publish_showcase` 组装 payload（遍历 records 取 `plan_item_snapshot`，附项目 `market_config`/`output_config`/`selling_points`）写入 showcase。
- `app/schemas.py`：`GalleryShowcaseRead` 加 `payload: dict`（可空兜底）。
- `app/gallery_routes.py`：`GET /showcases` 返回字典加 `payload`，移除未用的 `seed_showcases` 导入。
- `app/server.py`：startup 启动自愈 `_ensure_gallery_showcase_payload_column()`（inspect 防重复，MySQL=JSON NULL / SQLite=TEXT），绑定 `init_db()` 后调用。
- `app/migrations/showcase_payload.py`（新建）：独立可重复迁移，加 payload 列 + 删 6 行 SVG 假数据；已执行成功。

**前端** `web/src/pages/EcommerceGallery/index.tsx`
- 抽取共享 helper `applySnapshotsToProject({snapshots, globalOutput, marketConfig, sellingPoints})`：按 `type_id` 去重回填（已存在更新/不存在创建），回填全局 `output_config`/`market_config`/`selling_points`；有意不回填 `product_image`（源项目文件路径不可解析）。
- `handleSameStyle`（任务结果同款）复用该 helper。
- 新增 `handleSameStyleFromShowcase(sc)`：从 `sc.payload` 取参数回填。
- 卡片「生成同款」+ 详情弹窗「🎨 生成同款」按钮改调 `handleSameStyleFromShowcase`（原 `openDrawer()`）。
- `web/src/services/gallery.ts`：`GalleryShowcase` 接口加 `payload` 可选字段类型。

### 验证（全绿）
- 后端 `tests/verify_showcase_payload.py`：临时 SQLite 建表直调 `publish_showcase`，断言 payload 含 `plan_items/market_config/output_config/selling_points`；模拟 `GET /showcases` 字典返回。**PASS**。
- 前端 `vite build` 成功（修了一处 TS 简写误用 `market_config`/`selling_points` → 应为 `marketConfig`/`sellingPoints`）。
- 库实测：`gallery_showcases` 已含 `payload` 列，`showcase rows: 0`（6 条假数据已清空）。
- `server.py` 导入 + 自愈逻辑验证：idempotent 重复跑 OK；dialect=sqlite。

### 用户下一步
- 重新发布一次真实成图到创作案例 → 卡片/详情「生成同款」将回填完整参数。
- Docker 部署：跑 `python migrations/showcase_payload.py` 或靠 startup 自愈加列；前端仍需 `cd web && npm install && npm run build`。
- 总结文档：`overview_showcase_same_style_fix.md`。

---

## 电商套图 · 三处严重问题修复（慢 / 占位图 / 规格图留白）

用户反馈三个严重问题，先重新定位根因再实施（用户明确「先确认问题再设计再实施」）。

### 问题一（AI 生成提示词慢）——根因纠正
- 用户日志显示两次提示词调用各自独立（cn=351/cn=567，非一次性批量）、且相隔 ~32s，中间后端无 AI 活动 → 默认 `AI_PROMPT_BATCH_MODE=1`（单次批量）实际退化成「逐条调用」的兜底路径，被 AI 提供商限流（~30s 冷却）串行卡住。提示词单次本身很快(~1s)。
- 墙钟大头其实是出图阶段：原 `run_gallery_task` 逐张串行生成（`_real_generate` 单循环，每张超时上限 300s）。
- **不是**批量 A/B 切换问题。
- 改动：`_build_prompts_for_plan` 兜底逐条 → 改用 `ThreadPoolExecutor(max_workers=4)` 并发；`run_gallery_task` 阶段2 逐张串行 → 改为线程池并发（每线程独立 `SessionLocal()`，预取可序列化字段避免跨线程访问主会话对象），`max_workers=min(4, len(plan))`。

### 问题二（展示默认占位图）——根因：失败被静默吞掉
- `_real_generate` 原在「解析不到图片模型」(916-917) 与「模型返回空/异常」(955-971) 处静默 `return None` → 上层写离线占位 SVG，用户只看到占位图不知为何失败。
- 改动：`_real_generate` 改为返回 `{"error": 可读原因}`（不再返回 None）；两处静默分支补原因日志；新增自动兜底：prompt_en 超 1500 字符自动截断；带参考图失败自动去参考图重试一次。
- `run_gallery_task._generate_one`：成功判据改 `real and real.get("url")`；失败则 `rec.status="failed"` + `rec.error=原因`（不再给占位图）。
- 前端：`GalleryRecord` 接口加 `error?`；失败 cell 显示 `rec.error`（`gallery.css` 加 `.cell-failed-err` 样式）。
- **待用户验证**：重跑一次真实生成，看界面/日志报错原因（未配图片模型 vs 参考图/超时），以确认是否需加模型可用性自检。

### 问题三（规格参数图只留白、note 被画在图上）——根因纠正
- 原 spec 系统提示词**明令禁止画测量引导线**，AI 只产出「干净无文字」产品图 → 整体显空；`spec_overlay.py` 又把「补充说明 note」**直接渲染成图上文字**（用户明确反对）。
- 需求：note 只进 AI 提示词指导构图、**绝不画在图上**；画面应画出产品特性/尺寸标注（对齐参考图：衣长/胸围/袖长箭头+尺码表+比例剪影），不要留白。
- 改动：
  - `gallery_prompt_ai.py`：`_PROMPT_SYSTEM` 六、+ `build_user_config_text` spec 段 + `_PROMPT_BATCH_SYSTEM`/批量 per-item：从「禁止测量线」改为「允许淡淡测量引导线/指示点（无文字），右侧留浅灰面板，可含人体剪影；严禁文字/数字/表格」；note 仍经 `build_user_config_text`(261-264)/批量(588-590) 注入 prompt。
  - `spec_overlay.py`：删除 `overlay_spec`/`overlay_spec_image` 中渲染 note 的代码块；`_draw_measurement_lines` 标注线加粗(width2→3, head10→12)、改靛蓝更显眼；新增 `_draw_scale_silhouette()` 右侧面板底部画淡灰人体/比例剪影 + 「比例参考」标注（非用户文字）。

### 验证
- 后端 `py_compile` 三个文件全 OK。
- 前端 `npm run build` 成功（修了 `GalleryRecord` 缺 `error` 字段的 TS 报错）。
- `spec_overlay` 运行时烟雾测试因本地无 PIL/全量依赖栈未能跑，需 Docker/应用 venv 实测。
- 总结文档：`overview_three_issues_fix.md`（待写）。

### 用户下一步
- 重新构建前端 + 重启 api（`docker restart ai-agent-api` 或本地重启），实测三个问题。
- 问题二请贴一次真实生成的界面报错或 `docker logs ai-agent-api | grep -i "Image gen\|Real image generation"` 确认根因。

