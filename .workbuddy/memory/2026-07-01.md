# 2026-07-01 工作记录

## Bug 修复任务 — 项目前端崩溃问题

### 问题描述
用户报告页面出现 `form.resetFields is not a function` 错误，多个按钮和功能不可用，后端 API 返回 500 错误。

### 修复内容

1. **Ant Design `form.resetFields` 调用错误**（核心修复）
   - 修改 `AgentList/index.tsx:81`
   - 修改 `SkillManagement/index.tsx:65`
   - 修改 `MCPManagement/index.tsx:66`
   - 将 `form.resetFields({ key: value })` 改为 `form.setFieldsValue({ key: value }); form.resetFields()`

2. **后端 CORS 中间件**
   - 修改 `app/server.py` 添加 CORSMiddleware

3. **Dashboard 快捷链接路由不匹配**
   - 修改 `Dashboard/index.tsx:70` 将 `/agents` 改为 `/agents/manage`

4. **Settings API 表单绑定问题**
   - 移除无 `Form.useForm()` 绑定的空 `<Form>` 包裹

5. **AgentDetail 多处 bug**
   - 修复 Unicode 乱码（4 处）
   - 修复未定义变量 `err.detail`
   - 启用"新会话"按钮并添加功能

6. **请求错误处理优化**
   - 改进 `request.ts` 错误提示清晰度

### 涉及文件
- `web/src/pages/AgentList/index.tsx`
- `web/src/pages/SkillManagement/index.tsx`
- `web/src/pages/MCPManagement/index.tsx`
- `web/src/pages/Dashboard/index.tsx`
- `web/src/pages/Settings/index.tsx`
- `web/src/pages/AgentDetail/index.tsx`
- `web/src/services/request.ts`
- `app/server.py`

---

## 聊天界面重构 — PromptTemplate + Provider/Model Selector

### 需求
- 管理页改造为"提示词模板管理"（prompt-templates 路由）
- 聊天界面完全移除 Agent 相关的一切：
  - 侧栏顶部的 Agent 选择下拉框
  - Header 中的 Agent 名称显示
  - 发送消息时的 agent_id 参数
  - 管理页的 "新建 Agent" 按钮（已替换为 PromptTemplate）
- 聊天界面只用：Template（系统提示词）+ Provider/Model（API 凭证和模型）
- AI 提供商需要你手动配置，聊天时自动默认选择
- 在聊天输入框下方新增两行选择器：
  1. AI 提供商 + 模型下拉选择
  2. Prompt 模板下拉选择
- 新建会话按钮放在侧栏底部 + 输入框下方

### 已完成的后端修改

1. **app/models.py** — 新增 `PromptTemplate` 数据模型
   - 字段：name, slug, system_prompt, variables(JSON), category, description, enabled, is_default
   - 建立 user_id 外键关联

2. **app/schemas.py** — 新增 PromptTemplate CRUD schema
   - PromptTemplateCreate, PromptTemplateUpdate, PromptTemplateRead

3. **app/api.py** — 新增 PromptTemplate API 路由
   - GET/POST/PATCH/DELETE /api/prompt-templates
   - GET /api/prompt-templates/default
   - GET /api/providers-chat（精简版，返回提供商+聊天模型列表）
   - 修改 POST /api/chat 端点，支持 template_id, provider_id, model_name 参数

4. **app/agent.py** — 修改 ask_agent 函数
   - 优先级：template > agent > default
   - 支持从 Provider/ProviderModel 表加载 API 凭证
   - 支持动态选择模型
   - agent_id 改为可选参数

### 已完成的前端修改（彻底移除 Agent）

1. **web/src/pages/PromptTemplateList/index.tsx** — 新建提示词模板管理页
   - 表格展示所有模板
   - 支持增删改查、启用/禁用、设默认

2. **web/src/components/ChatSelector.tsx** — 新建选择器组件
   - Provider → Model 级联下拉
   - Template 下拉
   - 新建会话按钮

3. **web/src/stores/useChatSelectors.ts** — 新建 Zustand store
   - 持久化存储选择器状态
   - 使用 modelName（字符串）代替 modelId

4. **web/src/pages/ChatInterface/index.tsx** — 完全重构聊天界面
   - **彻底移除所有 Agent 相关**：
     - 删除 Agent interface 和 agents state
     - 删除 activeAgentId state
     - 删除侧栏顶部的 Agent 选择下拉框
     - 删除 Header 中的 Agent 名称（改为显示当前会话标题）
     - 删除发消息时的 agent_id 参数
     - 删除新建线程时的 agent_id 参数
   - 侧栏只有：会话列表 + "新建会话"按钮
   - 输入框下方：ChatSelector 组件（Provider/Model + Template）

5. **web/src/App.tsx** — 更新路由
   - `/agents/manage` 重定向到 `/prompt-templates`
   - 聊天路由指向 ChatInterface

6. **web/src/layouts/BasicLayout.tsx** — 更新侧边栏菜单
   - "Agent" 改为"聊天"，子菜单只有"对话"
   - 新增独立的"提示词模板"菜单项
   - 从"资源中心"移除重复的"提示词模板"

### 后续工作
1. 数据库初始化：运行 init_db() 创建 prompt_templates 表
2. 预置 2-3 个默认 PromptTemplate
3. 测试完整流程：创建模板 → 选择模板 → 发送消息
4. 考虑添加 Template 变量替换功能（{{variable}} 语法）

---

## 修复遗留 Bug

1. **ChatInterface 引用了已删除的 setAgents** — 移除了 fetch agents 的 useEffect
2. **Skill/MCP 菜单被误删** — 恢复 BasicLayout 菜单，将 MCP Server 和 Skills 移回"资源中心"子菜单
3. "聊天"菜单的 key 从 `/agents` 改为 `/chat`，避免与路由冲突

## 菜单结构调整

### 新菜单层级
- 仪表盘
- **Agent**（主模块）
  - 聊天
  - AI 提供商
  - MCP Server
  - Skills
  - 提示词模板
- **资源中心**
  - 知识库
- 用户管理
- 系统设置

## 聊天界面 - 侧栏折叠功能确认

### 已完成
- 侧栏宽度 240px，可折叠至 0px
- 折叠按钮在聊天区域 Header 左上角（☰/✕图标切换）
- 移动端（屏幕<768px）自动折叠
- 选择会话后自动折叠侧栏（移动端）
- Agent 选择器在侧栏顶部，会话列表在中间，新建会话按钮在侧栏底部
